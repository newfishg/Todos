destroy: function(e) {
    console.log('click');
    // var id = Number($(e.target).parents('tr').attr('data-id'));
    // delete entire tr from the table
    // this.remove();
    // delete this model from collection
    // App.todos.trigger('destroyTodo', id);
    // delete this data from data json
    // use ajax to sent request
    // $.ajax({
    //   url: 'delete/' + id,
    //   type: 'post',
    //   data: this.model.toJSON(),
    //   success: function() {
    //   }
    // });
    


    // re-render todosView
    // re-render nav View
  },